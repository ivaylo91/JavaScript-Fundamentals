/**
 *@author Ivaylo Penev
 */

const crystalOperation = function (input) {

    let targetSize = input[0];
    let oprStr = 'Cut';

    for (let i = 1; i < input.length - 1; i++) {

        let microns = input[i];

        console.log(`Processing chunk ${microns} microns`);
        
        
        function  cut() {
            
        }
    }
};
